{
  "name": "yiq",
  "license": "MIT",
  "version": "3.0.1",
  "description": "Returns light when a color is dark and dark when a color is light (and a little more).",
  "files": [
    "dist"
  ],
  "main": "dist/yiq.commonjs.js",
  "module": "dist/yiq.js",
  "typings": "dist/yiq.d.ts",
  "scripts": {
    "lint": "eslint --max-warnings=0 --ignore-path .gitignore --ignore-pattern '!.*.*' .",
    "test": "jest",
    "del-dist": "del dist",
    "build": "yarn del-dist && yarn lint && yarn test && tsc",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "del-cli": "^2.0.0",
    "eslint": "^7.3.1",
    "eslint-config-lffg": "^20.0.0",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.9.5"
  },
  "eslintConfig": {
    "extends": [
      "lffg"
    ],
    "rules": {
      "import/no-default-export": "off",
      "no-param-reassign": "off"
    }
  },
  "prettier": "eslint-config-lffg/prettier",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lffg/yiq.git"
  },
  "keywords": [
    "yiq",
    "color",
    "black",
    "white",
    "light",
    "dark",
    "color-convert"
  ],
  "author": "Luiz Felipe Gonçalves <https://luizfelipe.dev>",
  "bugs": {
    "url": "https://github.com/lffg/yiq/issues"
  },
  "homepage": "https://github.com/lffg/yiq#readme"
}
